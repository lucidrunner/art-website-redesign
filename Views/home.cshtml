
@using Umbraco.Extensions
@using Umbraco.Cms.Web.Common.PublishedModels;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<Home>
@{
    Layout = "master.cshtml";
    var backgroundImage = Model.HeroBackgroundImage != null ? Model.HeroBackgroundImage.Url() : String.Empty;
    int? artgalleryImage = Model.KonstgalleriBild?.Id ?? null;
    int? coursesImage = Model.KurserBild?.Id ?? null;
    int? portraitImage = Model.PortraettBild?.Id ?? null;
    int? restorationImage = Model.RestaureringBild?.Id ?? null;
    int? frameWorkshopImage = Model.RamverkstadBild?.Id ?? null;
}

<section class="section section--half-height background-image-full overlay overlay--dark section--content-center"
         style="background-image: url('@backgroundImage')">
    <div class="section__hero-content">
        <h1 class="hero-text">@Model.HeroHeader</h1>
        <p class="section__description">@Model.HeroDescription</p>
    </div>
</section>


<section class="container cardmenu">
    <div class="row">
        <div class="col-xs-12">
            @await Umbraco.RenderMacroAsync("FrontPageMenuItem", new { thumbnail = @artgalleryImage, label = "Konstgalleri", url = "konstgalleri" })
        </div>
    </div>
    <div class="row">
        <div class="col-xs-12 col-md-6">
            @await Umbraco.RenderMacroAsync("FrontPageMenuItem", new { thumbnail = @coursesImage, label = "Kurser", url = "kurser" })
        </div>

        <div class="col-xs-12 col-md-6">
            @await Umbraco.RenderMacroAsync("FrontPageMenuItem", new { thumbnail = @portraitImage, label = "Porträtt", url = "portratt" })
        </div>
    </div>
    
    <div class="row">
        <div class="col-xs-12 col-md-6">
            @await Umbraco.RenderMacroAsync("FrontPageMenuItem", new { thumbnail = @restorationImage, label = "Restaurering", url = "restaurering" })
        </div>

        <div class="col-xs-12 col-md-6">
            @await Umbraco.RenderMacroAsync("FrontPageMenuItem", new { thumbnail = @frameWorkshopImage, label = "Ramverkstad", url = "ram" })
        </div>
    </div>
</section>
@*
<section class="section section">
    @await Html.GetBlockGridHtmlAsync(Model, "bodyText")
</section>*@
@*
<section class="section section--themed">

    <div class="container">
        <div class="row">

            <div class="ta-center">
                <h2>@Model.FooterHeader</h2>
                <p class="section__description mw-640 ma-h-auto">@Model.FooterDescription</p>

                <a class="button button--border--light_solid" href="@Model.FooterCtalink.Url()">
                    @Model.FooterCtacaption
                </a>

            </div>

        </div>
    </div>

</section>*@